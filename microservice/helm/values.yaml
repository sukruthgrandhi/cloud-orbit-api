# Default values for cloud-orbit-api.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2
namespace: cloud-orbit-api
host: cloudorbit.eastus.cloudapp.azure.com
# host: mtonib.com

image:
  repository: sukruthgrandhi/cloud-orbit-api-image
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "1.1.19"
